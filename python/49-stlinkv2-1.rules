# Rule for the STM32 Nucleo board with STLink
# On adding the STLink CDC ACM device this rule sets read and write permissions
# to it (/dev/ttyACM0) and launches user script 'serialtest.sh'

# Usage: copy this file into the '/etc/udev/rules.d' directory

SUBSYSTEMS=="usb", ATTRS{idVendor}=="0483", ATTRS{idProduct}=="374b", \
	MODE:="0666", ACTION=="add", RUN+="/home/ebold/info_display/python/serialtest.sh"
